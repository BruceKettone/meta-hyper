MACHINE = "lkvm-arm64"
DISTRO = "poky"
TMPDIR .= "-guest-zram"

TCLIBC = "glibc"
DISTRO_FEATURES:append = " x11 alsa"
LICENSE_FLAGS_ACCEPTED += "commercial"

PREFERRED_PROVIDER_virtual/kernel:lkvm-arm64 = "linux-yocto-tiny"
HOST_SWAP_FEATURE = "zram.scc"
